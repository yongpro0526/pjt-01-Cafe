<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.miniproject.cafe.Mapper.CouponMapper">

    <!-- 사용 가능한 쿠폰 조회 -->
    <select id="getCouponsByUser" resultType="com.miniproject.cafe.VO.CouponVO">
        SELECT *
        FROM coupon
        WHERE member_id = #{memberId}
          AND used = 0
        ORDER BY expire_date ASC
    </select>

    <!-- 쿠폰 발급 -->
    <insert id="insertCoupon" parameterType="com.miniproject.cafe.VO.CouponVO">
        INSERT INTO coupon (member_id, coupon_name, coupon_type, expire_date, used)
        VALUES (#{memberId}, #{couponName}, #{couponType}, #{expireDate}, #{used})
    </insert>

    <!-- 쿠폰 사용 처리 -->
    <update id="markUsed">
        UPDATE coupon
        SET used = 1
        WHERE coupon_id = #{couponId}
    </update>

</mapper>
